[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "destrier-harness"
version = "2.0.0"
requires-python = ">=3.11"

[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "-q"
pythonpath = ["."]
